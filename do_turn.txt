function do_turn(turn_data):
	sequence is an empty list
	state  = turn_data
	if sequence is empty:
		set problem and set goal
		sequence = search(problem)
		if sequence is failure:
			return None
	return sequence.pop()


